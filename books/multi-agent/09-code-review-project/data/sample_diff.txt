diff --git a/api/auth.py b/api/auth.py
index 1234567..abcdefg 100644
--- a/api/auth.py
+++ b/api/auth.py
@@ -10,6 +10,15 @@ def login(username, password):
     """用户登录"""
+    # 临时实现：直接拼接 SQL，后续要优化
+    query = f"SELECT * FROM users WHERE name='{username}' AND pwd='{password}'"
+    result = db.execute(query)
+    if result:
+        return {"token": generate_token(result[0])}
+    return None
+
+def generate_token(user_id):
+    """生成 JWT token"""
+    import jwt
+    return jwt.encode({"user_id": user_id}, "secret_key_123", algorithm="HS256")
+
 def register(username, password):
     """用户注册"""
     # 已有实现
diff --git a/api/users.py b/api/users.py
index 2345678..bcdefgh 100644
--- a/api/users.py
+++ b/api/users.py
@@ -5,6 +5,12 @@ def get_user(user_id):
     """获取用户信息"""
+    # 注意：没鉴权，任何人都能查任意用户
+    user = db.query(f"SELECT * FROM users WHERE id={user_id}")
+    return user
+
+def update_user(user_id, data):
+    """更新用户"""
+    db.execute(f"UPDATE users SET name='{data['name']}' WHERE id={user_id}")
+    return {"success": True}
+
 def list_users():
     """列出所有用户"""
     # 已有实现
diff --git a/tests/test_auth.py b/tests/test_auth.py
index 3456789..cdefghi 100644
--- a/tests/test_auth.py
+++ b/tests/test_auth.py
@@ -1,3 +1,5 @@
 def test_login_success():
     assert login("alice", "123456") is not None
+
+# 没有测试 SQL 注入、密码错误、token 过期等场景