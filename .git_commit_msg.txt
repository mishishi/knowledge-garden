feat(recap): weekly digest 复制为 Markdown (发邮件 / 笔记用)

原 Tier 三 #2 每周回顾邮件需要后端 (SendGrid + auth), side project 不划算.
替代: in-app 复制为 Markdown, 用户可手动贴到邮件 / 笔记 / 微信.

实现:
- weekly-recap 加 "复制" 按钮 (recap-tabs 右侧)
- buildRecapMarkdown() 生成 # 知识花园 · 本周阅读摘要 + summary + 日期
- navigator.clipboard.writeText (主路径) + textarea fallback (旧浏览器)
- 反馈: 复制成功变绿色 "已复制" 1.5s

涉及 commit:
- extract.js 重新入仓 (从 trash 恢复, build 验证 JS 用)
- knowledge_index.json / rss.xml / sitemap.xml auto-regen